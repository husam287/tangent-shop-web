import classes from "@/utils/classes";
import { {{extractName name}}Props } from "./types";
import styles from './styles.module.scss'

export default function {{extractName name}}({ }: {{extractName name}}Props) {
  return (
    <div className={classes([styles.container])} data-testid="{{extractName name}}">
      {{extractName name}}
    </div>
  );
}
