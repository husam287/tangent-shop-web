import type { Meta, StoryObj } from '@storybook/react';
import {{extractName name}} from ".";

const meta: Meta<typeof {{extractName name}}> = {
    title: "{{extractStoryPath name}}",
    component: {{extractName name}},
};

export default meta;
type Story = StoryObj<typeof {{extractName name}}>;

export const Primary: Story = {
    args: {
        
    }
};